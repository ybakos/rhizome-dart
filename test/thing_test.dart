import 'package:test/test.dart';
import 'package:rhizome/rhizome.dart';

void main() {
  group('A group of tests', () {
    Thing thing;

    setUp(() {
      thing = Thing();
    });

    test('A Thing exists', () {
      // no op
    });
  });
}
